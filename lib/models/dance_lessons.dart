import 'package:i_dance/models/dance_file.dart';

class DanceLessons extends DanceFile {
  DanceLessons(
      {required super.title,
      required super.content,
      required super.createdAt,
      required super.updatedAt});
}
